import { View } from "react-native";

type TrieNode = {
    children: Map<string, TrieNode>;
    id: number;
}

class Trie {
}

export default function SearchBar() {
    return (
        <View>
        </View>
    );
}
